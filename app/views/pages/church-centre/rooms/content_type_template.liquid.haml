---
title: Room template

# true if the page is included in the menu
listed: false

# true if the page is published
published: true

# position among sibling pages
position: 99

# other unique identifier of this page. To be used with the path_to liquid tag.
# handle: my-page-handle

# content type that this page is templatizing
content_type: "rooms"
---
{% extends parent %}

{% block content %}
%section
  %div
    .room
      %h2 {{ room.name | escape }}
      %small approx {{ room.dimentions | escape }} ({{ room.capacity | escape }} people)
      {% if room.photo.url %}
      .room-image
        %img{ src: "{{ room.photo.url | resize: '500x300#' }}", width: "500", height: "300" }
      {% endif %}
      {{ room.description | markdown }}
{% if room.photo.url %}
%section.image{ style: "background-image: url({{ room.photo.url }}); background-position: center; background-size: cover;"}
{% endif %}

{% endblock %}
